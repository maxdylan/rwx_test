<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="300dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorPrimaryDark">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/ivBigIcon"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvFirstLine"
        android:text="第一行文字"
        android:textSize="40sp"
        android:textColor="#fff"
        app:layout_constraintTop_toTopOf="@id/ivBigIcon"
        app:layout_constraintLeft_toRightOf="@+id/ivBigIcon"
        android:layout_marginStart="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvSecondLine"
        android:text="第二行文字"
        android:textSize="30sp"
        android:textColor="#fff"
        app:layout_constraintTop_toBottomOf="@+id/tvFirstLine"
        app:layout_constraintLeft_toLeftOf="@+id/tvFirstLine"
        android:layout_marginTop="20dp" />


</androidx.constraintlayout.widget.ConstraintLayout>