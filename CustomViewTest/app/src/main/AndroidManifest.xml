<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lt.customviewtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MaterialEditTextActivity"></activity>
        <activity android:name=".activity.PointAnimatorActivity" />
        <activity android:name=".activity.StringAnimatorActivity" />
        <activity android:name=".activity.FlipAnimatorActivity" />
        <activity android:name=".activity.ScaleCircleAnimActivity" />
        <activity android:name=".activity.AnimatorActivity" />
        <activity android:name=".activity.CameraActivity" />
        <activity android:name=".activity.TextImgActivity" />
        <activity android:name=".activity.TextCenterActivity" />
        <activity android:name=".activity.AvatorActivity" />
        <activity android:name=".activity.PieActivity" />
        <activity android:name=".activity.DashboardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>